#include <stdio.h>
#include<string.h>

int main() {
          char s1[50],s2[50];
          
          scanf("%s",s1);
          scanf("%s",s2);
     int i=strcmp(s1,s2);
    if(i==0)
    {
        printf("Strings are equal");
    }
    else
    {
        printf("strings are not equal");
    }
}

Input:
java 
input:
python
output:
Strings are not equal


Input:
java
In[put:
  java

  output:
  Strings are equal
          
