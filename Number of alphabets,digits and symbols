//Program on read a string and print number of alphabets,digits and symbols


#include <stdio.h>
#include<string.h>
int main() {
          char s[100];
          int i,alp=0,dig=0,sym=0;
         scanf("%[^\n]s",s);
   for(i=0;s[i]!='\0';i++)
   {
     if((s[i]>=65 && s[i]<=90) || (s[i]>=97 && s[i]<=122))
        {
            alp++;
        }
        else if((s[i]>=48 && s[i]<=57))
            {
                dig++;
            }
            else{
                sym++;
            }
        }
        printf("%d\n%d\n%d",alp,dig,sym);
}

Inout:
pooja @16

output:
5
2
2
